/* AUTO-GENERATED by scripts/gen-md3-validate.py - DO NOT EDIT */
/* Extended MD3 validators generated from src/md3-spec.dsl */

#ifndef MD3_VALIDATE_GEN_INC
#define MD3_VALIDATE_GEN_INC

#include "md3-validate.h" /* For md3_violation_t, md3_dp_to_px, etc. */


/* MD3 Shape Tokens (corner radii in dp) */
#define MD3_SHAPE_NONE 0.0f
#define MD3_SHAPE_EXTRA_SMALL 4.0f
#define MD3_SHAPE_SMALL 8.0f
#define MD3_SHAPE_MEDIUM 12.0f
#define MD3_SHAPE_LARGE 16.0f
#define MD3_SHAPE_EXTRA_LARGE 28.0f

/* Exact size check (returns MD3_SIZE_MISMATCH if outside tolerance) */
static inline md3_violation_t md3_check_exact(int px, float dp, int tol, float scale)
{
    int expected = md3_dp_to_px(scale, dp);
    int diff = px - expected;
    return (diff < -tol || diff > tol) ? MD3_SIZE_MISMATCH : MD3_OK;
}

/* Minimum size check with configurable flag */
static inline md3_violation_t md3_check_min(int px, float dp, float scale, md3_violation_t flag)
{
    int min = md3_dp_to_px(scale, dp);
    return (px < min) ? flag : MD3_OK;
}

static inline md3_violation_t md3_check_exact_dp(int px, float dp, float scale, md3_violation_t flag)
{
    int expected = md3_dp_to_px(scale, dp);
    return (px != expected) ? flag : MD3_OK;
}

static inline md3_violation_t md3_check_touch_target_dp(int w, int h, float scale, float dp)
{
    int min = md3_dp_to_px(scale, dp);
    return (w < min || h < min) ? MD3_TOUCH_TARGET : MD3_OK;
}

static inline md3_violation_t md3_check_corner_radius(int px, float dp, float scale)
{
    return md3_check_exact_dp(px, dp, scale, MD3_CORNER_RADIUS);
}

static inline md3_violation_t md3_check_icon_size(int px, float dp, float scale)
{
    return md3_check_exact_dp(px, dp, scale, MD3_ICON_SIZE);
}

static inline md3_violation_t md3_check_padding(int px, float dp, float scale)
{
    return md3_check_exact_dp(px, dp, scale, MD3_PADDING);
}

static inline md3_violation_t md3_check_gap(int px, float dp, float scale)
{
    return md3_check_exact_dp(px, dp, scale, MD3_GAP);
}

static inline md3_violation_t md3_check_indicator(int px, float dp, float scale)
{
    return md3_check_exact_dp(px, dp, scale, MD3_INDICATOR);
}

/* Button validator */
static inline md3_violation_t md3_check_button_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, 20.0f, scale);
}
static inline md3_violation_t md3_check_button_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}

/* Segmented validator */
static inline md3_violation_t md3_check_segmented(int height_px, float scale)
{
    return md3_check_exact(height_px, 40.0f, 0, scale);
}
static inline md3_violation_t md3_check_segmented_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_segmented_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 18.0f, scale);
}

/* Fab Small validator */
static inline md3_violation_t md3_check_fab_small(int size_px, float scale)
{
    return md3_check_exact(size_px, 40.0f, 1, scale);
}
static inline md3_violation_t md3_check_fab_small_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_MEDIUM, scale);
}
static inline md3_violation_t md3_check_fab_small_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_fab_small_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}

/* Fab validator */
static inline md3_violation_t md3_check_fab_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_LARGE, scale);
}
static inline md3_violation_t md3_check_fab_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_fab_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}

/* Fab Large validator */
static inline md3_violation_t md3_check_fab_large_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_LARGE, scale);
}
static inline md3_violation_t md3_check_fab_large_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_fab_large_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 36.0f, scale);
}

/* Fab Extended validator */
static inline md3_violation_t md3_check_fab_extended(int height_px, float scale)
{
    return md3_check_min(height_px, 56.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_fab_extended_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_LARGE, scale);
}
static inline md3_violation_t md3_check_fab_extended_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_fab_extended_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_fab_extended_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_fab_extended_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}

/* Chip validator */
static inline md3_violation_t md3_check_chip_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_SMALL, scale);
}
static inline md3_violation_t md3_check_chip_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_chip_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 18.0f, scale);
}
static inline md3_violation_t md3_check_chip_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_chip_icon_label_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}

/* Textfield validator */
static inline md3_violation_t md3_check_textfield_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_SMALL, scale);
}
static inline md3_violation_t md3_check_textfield_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}

/* Icon Button validator */
static inline md3_violation_t md3_check_icon_button(int size_px, float scale)
{
    return md3_check_exact(size_px, 40.0f, 1, scale);
}
static inline md3_violation_t md3_check_icon_button_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, 20.0f, scale);
}
static inline md3_violation_t md3_check_icon_button_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_icon_button_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}

/* Slider validator */
static inline md3_violation_t md3_check_slider_track(int height_px, float scale)
{
    return md3_check_exact(height_px, 4.0f, 0, scale);
}
static inline md3_violation_t md3_check_slider_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_slider_thumb(int size_px, int is_pressed, float scale)
{
    float dp = is_pressed ? 28.0f : 20.0f;
    int expected = md3_dp_to_px(scale, dp);
    return (size_px != expected) ? MD3_THUMB_SIZE : MD3_OK;
}

/* Range Slider validator */
static inline md3_violation_t md3_check_range_slider_track(int height_px, float scale)
{
    return md3_check_exact(height_px, 4.0f, 0, scale);
}
static inline md3_violation_t md3_check_range_slider_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_range_slider_thumb(int size_px, int is_pressed, float scale)
{
    float dp = is_pressed ? 28.0f : 20.0f;
    int expected = md3_dp_to_px(scale, dp);
    return (size_px != expected) ? MD3_THUMB_SIZE : MD3_OK;
}

/* Switch validator */
static inline md3_violation_t md3_check_switch_track(int w, int h, float scale)
{
    int exp_w = md3_dp_to_px(scale, 52.0f);
    int exp_h = md3_dp_to_px(scale, 32.0f);
    return (w != exp_w || h != exp_h) ? MD3_SIZE_MISMATCH : MD3_OK;
}
static inline md3_violation_t md3_check_switch_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_LARGE, scale);
}
static inline md3_violation_t md3_check_switch_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_switch_thumb(int size_px, float scale)
{
    int expected = md3_dp_to_px(scale, 24.0f);
    return (size_px != expected) ? MD3_THUMB_SIZE : MD3_OK;
}

/* Tab validator */
static inline md3_violation_t md3_check_tab(int height_px, float scale)
{
    return md3_check_min(height_px, 48.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_tab_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_tab_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_tab_width(int width_px, float scale)
{
    return md3_check_min(width_px, 90.0f, scale, MD3_WIDTH_LOW);
}
static inline md3_violation_t md3_check_tab_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_tab_indicator(int height_px, float scale)
{
    return md3_check_indicator(height_px, 3.0f, scale);
}
static inline md3_violation_t md3_check_tab_icon_label_gap(int px, float scale)
{
    return md3_check_gap(px, 4.0f, scale);
}

/* Search Bar validator */
static inline md3_violation_t md3_check_search_bar(int height_px, float scale)
{
    return md3_check_min(height_px, 56.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_search_bar_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, 28.0f, scale);
}
static inline md3_violation_t md3_check_search_bar_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_search_bar_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_search_bar_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_search_bar_icon_label_gap(int px, float scale)
{
    return md3_check_gap(px, 16.0f, scale);
}

/* Menu Item validator */
static inline md3_violation_t md3_check_menu_item(int height_px, float scale)
{
    return md3_check_min(height_px, 40.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_menu_item_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}

/* Appbar Small validator */
static inline md3_violation_t md3_check_appbar_small(int height_px, float scale)
{
    return md3_check_exact(height_px, 64.0f, 0, scale);
}
static inline md3_violation_t md3_check_appbar_small_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_appbar_small_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_appbar_small_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_appbar_small_action_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}
static inline md3_violation_t md3_check_appbar_small_collapsed_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 64.0f, scale, MD3_HEIGHT_LOW);
}

/* Appbar Medium validator */
static inline md3_violation_t md3_check_appbar_medium(int height_px, float scale)
{
    return md3_check_exact(height_px, 112.0f, 0, scale);
}
static inline md3_violation_t md3_check_appbar_medium_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_appbar_medium_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_appbar_medium_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_appbar_medium_action_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}
static inline md3_violation_t md3_check_appbar_medium_collapsed_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 64.0f, scale, MD3_HEIGHT_LOW);
}

/* Appbar Large validator */
static inline md3_violation_t md3_check_appbar_large(int height_px, float scale)
{
    return md3_check_exact(height_px, 152.0f, 0, scale);
}
static inline md3_violation_t md3_check_appbar_large_touch(int w, int h, float scale)
{
    return md3_check_touch_target_dp(w, h, scale, 48.0f);
}
static inline md3_violation_t md3_check_appbar_large_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_appbar_large_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_appbar_large_action_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}
static inline md3_violation_t md3_check_appbar_large_collapsed_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 64.0f, scale, MD3_HEIGHT_LOW);
}

/* Date Picker Day validator */
static inline md3_violation_t md3_check_date_picker_day(int size_px, float scale)
{
    return md3_check_exact(size_px, 40.0f, 0, scale);
}
static inline md3_violation_t md3_check_date_picker_day_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, 20.0f, scale);
}

/* Time Picker Dial validator */
static inline md3_violation_t md3_check_time_picker_dial(int size_px, float scale)
{
    return md3_check_exact(size_px, 256.0f, 0, scale);
}
static inline md3_violation_t md3_check_time_picker_dial_selector(int size_px, float scale)
{
    return md3_check_exact_dp(size_px, 40.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_time_picker_dial_center_dot(int size_px, float scale)
{
    return md3_check_exact_dp(size_px, 8.0f, scale, MD3_SIZE_MISMATCH);
}

/* Time Picker Display validator */
static inline md3_violation_t md3_check_time_picker_display_width(int width_px, float scale)
{
    return md3_check_exact(width_px, 96.0f, 0, scale);
}
static inline md3_violation_t md3_check_time_picker_display_padding(int px, float scale)
{
    return md3_check_padding(px, 24.0f, scale);
}
static inline md3_violation_t md3_check_time_picker_display_header_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 80.0f, scale, MD3_HEIGHT_LOW);
}

/* Search View validator */
static inline md3_violation_t md3_check_search_view_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_search_view_padding(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_search_view_header_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 72.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_search_view_suggestion_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 56.0f, scale, MD3_HEIGHT_LOW);
}

/* Dialog validator */
static inline md3_violation_t md3_check_dialog_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_LARGE, scale);
}
static inline md3_violation_t md3_check_dialog_width(int width_px, float scale)
{
    return md3_check_min(width_px, 280.0f, scale, MD3_WIDTH_LOW);
}
static inline md3_violation_t md3_check_dialog_padding(int px, float scale)
{
    return md3_check_padding(px, 24.0f, scale);
}
static inline md3_violation_t md3_check_dialog_button_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 36.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_dialog_action_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}
static inline md3_violation_t md3_check_dialog_content_spacing(int px, float scale)
{
    return md3_check_gap(px, 16.0f, scale);
}

/* Fullscreen Dialog validator */
static inline md3_violation_t md3_check_fullscreen_dialog_padding(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_fullscreen_dialog_header_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 56.0f, scale, MD3_HEIGHT_LOW);
}

/* Bottom Sheet validator */
static inline md3_violation_t md3_check_bottom_sheet_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_LARGE, scale);
}
static inline md3_violation_t md3_check_bottom_sheet_drag_handle_width(int width_px, float scale)
{
    return md3_check_exact_dp(width_px, 32.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_bottom_sheet_drag_handle_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 4.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_bottom_sheet_min_height(int height_px, float scale)
{
    return md3_check_min(height_px, 64.0f, scale, MD3_HEIGHT_LOW);
}

/* Card validator */
static inline md3_violation_t md3_check_card_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_MEDIUM, scale);
}
static inline md3_violation_t md3_check_card_padding(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}

/* Snackbar validator */
static inline md3_violation_t md3_check_snackbar(int height_px, float scale)
{
    return md3_check_exact(height_px, 48.0f, 0, scale);
}
static inline md3_violation_t md3_check_snackbar_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_SMALL, scale);
}
static inline md3_violation_t md3_check_snackbar_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}

/* Tooltip validator */
static inline md3_violation_t md3_check_tooltip_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_SMALL, scale);
}
static inline md3_violation_t md3_check_tooltip_width(int width_px, float scale)
{
    return md3_check_min(width_px, 32.0f, scale, MD3_WIDTH_LOW);
}
static inline md3_violation_t md3_check_tooltip_padding(int px, float scale)
{
    return md3_check_padding(px, 12.0f, scale);
}
static inline md3_violation_t md3_check_tooltip_min_height(int height_px, float scale)
{
    return md3_check_min(height_px, 32.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_tooltip_offset(int px, float scale)
{
    return md3_check_exact_dp(px, 8.0f, scale, MD3_PADDING);
}

/* Banner validator */
static inline md3_violation_t md3_check_banner(int height_px, float scale)
{
    return md3_check_min(height_px, 52.0f, scale, MD3_HEIGHT_LOW);
}
static inline md3_violation_t md3_check_banner_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_banner_padding(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_banner_action_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}

/* Nav Rail validator */
static inline md3_violation_t md3_check_nav_rail_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_LARGE, scale);
}
static inline md3_violation_t md3_check_nav_rail_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_nav_rail_width(int width_px, float scale)
{
    return md3_check_exact(width_px, 80.0f, 0, scale);
}
static inline md3_violation_t md3_check_nav_rail_item_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 56.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_nav_rail_indicator(int height_px, float scale)
{
    return md3_check_indicator(height_px, 32.0f, scale);
}
static inline md3_violation_t md3_check_nav_rail_indicator_width(int width_px, float scale)
{
    return md3_check_exact_dp(width_px, 56.0f, scale, MD3_INDICATOR);
}
static inline md3_violation_t md3_check_nav_rail_expanded_width(int width_px, float scale)
{
    return md3_check_exact_dp(width_px, 256.0f, scale, MD3_SIZE_MISMATCH);
}

/* Nav Drawer validator */
static inline md3_violation_t md3_check_nav_drawer_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_nav_drawer_width(int width_px, float scale)
{
    return md3_check_exact(width_px, 280.0f, 0, scale);
}
static inline md3_violation_t md3_check_nav_drawer_padding_h(int px, float scale)
{
    return md3_check_padding(px, 12.0f, scale);
}
static inline md3_violation_t md3_check_nav_drawer_item_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 56.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_nav_drawer_icon_label_gap(int px, float scale)
{
    return md3_check_gap(px, 12.0f, scale);
}

/* Nav Bar validator */
static inline md3_violation_t md3_check_nav_bar(int height_px, float scale)
{
    return md3_check_exact(height_px, 80.0f, 0, scale);
}
static inline md3_violation_t md3_check_nav_bar_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_nav_bar_indicator(int height_px, float scale)
{
    return md3_check_indicator(height_px, 32.0f, scale);
}
static inline md3_violation_t md3_check_nav_bar_indicator_width(int width_px, float scale)
{
    return md3_check_exact_dp(width_px, 64.0f, scale, MD3_INDICATOR);
}
static inline md3_violation_t md3_check_nav_bar_icon_label_gap(int px, float scale)
{
    return md3_check_gap(px, 4.0f, scale);
}

/* Bottom App Bar validator */
static inline md3_violation_t md3_check_bottom_app_bar(int height_px, float scale)
{
    return md3_check_exact(height_px, 80.0f, 0, scale);
}
static inline md3_violation_t md3_check_bottom_app_bar_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_bottom_app_bar_icon_container(int size_px, float scale)
{
    return md3_check_exact_dp(size_px, 48.0f, scale, MD3_SIZE_MISMATCH);
}

/* Menu validator */
static inline md3_violation_t md3_check_menu_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_SMALL, scale);
}
static inline md3_violation_t md3_check_menu_width(int width_px, float scale)
{
    return md3_check_min(width_px, 200.0f, scale, MD3_WIDTH_LOW);
}
static inline md3_violation_t md3_check_menu_padding_h(int px, float scale)
{
    return md3_check_padding(px, 12.0f, scale);
}
static inline md3_violation_t md3_check_menu_padding_v(int px, float scale)
{
    return md3_check_padding(px, 8.0f, scale);
}

/* List Item One Line validator */
static inline md3_violation_t md3_check_list_item_one_line(int height_px, float scale)
{
    return md3_check_exact(height_px, 56.0f, 0, scale);
}
static inline md3_violation_t md3_check_list_item_one_line_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_list_item_one_line_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_list_item_one_line_avatar(int size_px, float scale)
{
    return md3_check_exact_dp(size_px, 40.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_list_item_one_line_divider_inset(int px, float scale)
{
    return md3_check_exact_dp(px, 16.0f, scale, MD3_PADDING);
}

/* List Item Two Line validator */
static inline md3_violation_t md3_check_list_item_two_line(int height_px, float scale)
{
    return md3_check_exact(height_px, 72.0f, 0, scale);
}
static inline md3_violation_t md3_check_list_item_two_line_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_list_item_two_line_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_list_item_two_line_avatar(int size_px, float scale)
{
    return md3_check_exact_dp(size_px, 40.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_list_item_two_line_divider_inset(int px, float scale)
{
    return md3_check_exact_dp(px, 16.0f, scale, MD3_PADDING);
}

/* List Item Three Line validator */
static inline md3_violation_t md3_check_list_item_three_line(int height_px, float scale)
{
    return md3_check_exact(height_px, 88.0f, 0, scale);
}
static inline md3_violation_t md3_check_list_item_three_line_icon(int size_px, float scale)
{
    return md3_check_icon_size(size_px, 24.0f, scale);
}
static inline md3_violation_t md3_check_list_item_three_line_padding_h(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}
static inline md3_violation_t md3_check_list_item_three_line_avatar(int size_px, float scale)
{
    return md3_check_exact_dp(size_px, 40.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_list_item_three_line_divider_inset(int px, float scale)
{
    return md3_check_exact_dp(px, 16.0f, scale, MD3_PADDING);
}

/* Data Table Header validator */
static inline md3_violation_t md3_check_data_table_header(int height_px, float scale)
{
    return md3_check_exact(height_px, 56.0f, 0, scale);
}
static inline md3_violation_t md3_check_data_table_header_padding(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}

/* Data Table Row validator */
static inline md3_violation_t md3_check_data_table_row(int height_px, float scale)
{
    return md3_check_exact(height_px, 52.0f, 0, scale);
}
static inline md3_violation_t md3_check_data_table_row_padding(int px, float scale)
{
    return md3_check_padding(px, 16.0f, scale);
}

/* Badge Dot validator */
static inline md3_violation_t md3_check_badge_dot(int size_px, float scale)
{
    return md3_check_exact(size_px, 8.0f, 0, scale);
}

/* Badge Label validator */
static inline md3_violation_t md3_check_badge_label(int height_px, float scale)
{
    return md3_check_exact(height_px, 16.0f, 0, scale);
}
static inline md3_violation_t md3_check_badge_label_padding_h(int px, float scale)
{
    return md3_check_padding(px, 4.0f, scale);
}

/* Progress Bar validator */
static inline md3_violation_t md3_check_progress_bar(int height_px, float scale)
{
    return md3_check_exact(height_px, 4.0f, 0, scale);
}

/* Dropdown validator */
static inline md3_violation_t md3_check_dropdown(int height_px, float scale)
{
    return md3_check_exact(height_px, 56.0f, 0, scale);
}
static inline md3_violation_t md3_check_dropdown_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_SMALL, scale);
}
static inline md3_violation_t md3_check_dropdown_item_height(int height_px, float scale)
{
    return md3_check_exact_dp(height_px, 48.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_dropdown_max_height(int height_px, float scale)
{
    int max_h = md3_dp_to_px(scale, 280.0f);
    return (height_px > max_h) ? MD3_HEIGHT_LOW : MD3_OK;
}

/* Side Sheet validator */
static inline md3_violation_t md3_check_side_sheet_width(int width_px, float scale)
{
    return md3_check_exact(width_px, 400.0f, 0, scale);
}
static inline md3_violation_t md3_check_side_sheet_padding(int px, float scale)
{
    return md3_check_padding(px, 24.0f, scale);
}

/* Carousel validator */
static inline md3_violation_t md3_check_carousel_corner(int radius_px, float scale)
{
    return md3_check_corner_radius(radius_px, MD3_SHAPE_EXTRA_LARGE, scale);
}
static inline md3_violation_t md3_check_carousel_item_width(int width_px, float scale)
{
    return md3_check_exact_dp(width_px, 240.0f, scale, MD3_SIZE_MISMATCH);
}
static inline md3_violation_t md3_check_carousel_gap(int px, float scale)
{
    return md3_check_gap(px, 8.0f, scale);
}

/* MD3 Typography Scale (font sizes in dp) */
#define MD3_TYPO_HEADLINE_SMALL 24.0f
#define MD3_TYPO_TITLE_LARGE 22.0f
#define MD3_TYPO_TITLE_MEDIUM 16.0f
#define MD3_TYPO_TITLE_SMALL 14.0f
#define MD3_TYPO_BODY_LARGE 16.0f
#define MD3_TYPO_BODY_MEDIUM 14.0f
#define MD3_TYPO_BODY_SMALL 12.0f
#define MD3_TYPO_LABEL_LARGE 14.0f
#define MD3_TYPO_LABEL_MEDIUM 12.0f
#define MD3_TYPO_LABEL_SMALL 11.0f

/* State layer opacity validation */
/* Expected alpha values from DSL: hover=20, focus=30, press=30, drag=40, disable=96 */
/* See md3_check_state_alpha() in md3-validate.h */

#endif /* MD3_VALIDATE_GEN_INC */
